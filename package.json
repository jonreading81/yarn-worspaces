{
    "private": true,
    "name": "yarn-monorepo",
    "workspaces": [
        "apps/*",
        "libs/*"
    ],
    "scripts": {
        "compare": "node ./scripts/compare.js $*"
    },
    "devDependencies": {
        "check-if-folder-contents-changed-in-git-commit-range": "^1.0.1"
    },
    "dependencies": {
    }
}
